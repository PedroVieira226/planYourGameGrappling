spring:
  application:
    name: PlanYourGame

  datasource:
    url: jdbc:postgresql://localhost:5433/planyourgame
    username: postgres
    password: "25252525@Aa"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: never